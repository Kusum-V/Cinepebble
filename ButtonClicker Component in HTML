<!DOCTYPE html>
<html>
<head>
    <title>Button Clicker</title>
</head>
<body>
    <div id="root"></div>
    <script>
        function ButtonClicker() {
            function handleClick() {
                alert('Button Clicked!');
            }

            return <button onClick={handleClick}>Click me</button>;
        }

        ReactDOM.render(<ButtonClicker />, document.getElementById('root'));
    </script>
</body>
</html>
